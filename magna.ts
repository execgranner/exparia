type Key = 'K';

function processKey(key: Key): void {
  // Assuming 'K' is a part of some operation
  console.log(`Processing key: ${key}`);
  // Further processing logic can be added here
}

// Usage
const key: Key = 'K';
processKey(key);
